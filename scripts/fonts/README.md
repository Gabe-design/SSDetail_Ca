TTF copies used only by `scripts/build-brand.mjs` to convert logo/wordmark text
into SVG paths (favicon, logo mark, OG image). All are SIL Open Font License:
Playfair Display, Oswald, Work Sans. The site itself loads fonts from
@fontsource-variable packages.
